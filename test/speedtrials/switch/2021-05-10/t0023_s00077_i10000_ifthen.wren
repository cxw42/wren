for(iter in 1..10000) {
  var topic = "a" // can't possibly match
  if(false) { // so the loop below can be regular
  } else if("1".contains(topic)) {
    Fiber.abort("matched 1")
  } else if("2".contains(topic)) {
    Fiber.abort("matched 2")
  } else if("3".contains(topic)) {
    Fiber.abort("matched 3")
  } else if("4".contains(topic)) {
    Fiber.abort("matched 4")
  } else if("5".contains(topic)) {
    Fiber.abort("matched 5")
  } else if("6".contains(topic)) {
    Fiber.abort("matched 6")
  } else if("7".contains(topic)) {
    Fiber.abort("matched 7")
  } else if("8".contains(topic)) {
    Fiber.abort("matched 8")
  } else if("9".contains(topic)) {
    Fiber.abort("matched 9")
  } else if("10".contains(topic)) {
    Fiber.abort("matched 10")
  } else if("11".contains(topic)) {
    Fiber.abort("matched 11")
  } else if("12".contains(topic)) {
    Fiber.abort("matched 12")
  } else if("13".contains(topic)) {
    Fiber.abort("matched 13")
  } else if("14".contains(topic)) {
    Fiber.abort("matched 14")
  } else if("15".contains(topic)) {
    Fiber.abort("matched 15")
  } else if("16".contains(topic)) {
    Fiber.abort("matched 16")
  } else if("17".contains(topic)) {
    Fiber.abort("matched 17")
  } else if("18".contains(topic)) {
    Fiber.abort("matched 18")
  } else if("19".contains(topic)) {
    Fiber.abort("matched 19")
  } else if("20".contains(topic)) {
    Fiber.abort("matched 20")
  } else if("21".contains(topic)) {
    Fiber.abort("matched 21")
  } else if("22".contains(topic)) {
    Fiber.abort("matched 22")
  } else if("23".contains(topic)) {
    Fiber.abort("matched 23")
  } else if("24".contains(topic)) {
    Fiber.abort("matched 24")
  } else if("25".contains(topic)) {
    Fiber.abort("matched 25")
  } else if("26".contains(topic)) {
    Fiber.abort("matched 26")
  } else if("27".contains(topic)) {
    Fiber.abort("matched 27")
  } else if("28".contains(topic)) {
    Fiber.abort("matched 28")
  } else if("29".contains(topic)) {
    Fiber.abort("matched 29")
  } else if("30".contains(topic)) {
    Fiber.abort("matched 30")
  } else if("31".contains(topic)) {
    Fiber.abort("matched 31")
  } else if("32".contains(topic)) {
    Fiber.abort("matched 32")
  } else if("33".contains(topic)) {
    Fiber.abort("matched 33")
  } else if("34".contains(topic)) {
    Fiber.abort("matched 34")
  } else if("35".contains(topic)) {
    Fiber.abort("matched 35")
  } else if("36".contains(topic)) {
    Fiber.abort("matched 36")
  } else if("37".contains(topic)) {
    Fiber.abort("matched 37")
  } else if("38".contains(topic)) {
    Fiber.abort("matched 38")
  } else if("39".contains(topic)) {
    Fiber.abort("matched 39")
  } else if("40".contains(topic)) {
    Fiber.abort("matched 40")
  } else if("41".contains(topic)) {
    Fiber.abort("matched 41")
  } else if("42".contains(topic)) {
    Fiber.abort("matched 42")
  } else if("43".contains(topic)) {
    Fiber.abort("matched 43")
  } else if("44".contains(topic)) {
    Fiber.abort("matched 44")
  } else if("45".contains(topic)) {
    Fiber.abort("matched 45")
  } else if("46".contains(topic)) {
    Fiber.abort("matched 46")
  } else if("47".contains(topic)) {
    Fiber.abort("matched 47")
  } else if("48".contains(topic)) {
    Fiber.abort("matched 48")
  } else if("49".contains(topic)) {
    Fiber.abort("matched 49")
  } else if("50".contains(topic)) {
    Fiber.abort("matched 50")
  } else if("51".contains(topic)) {
    Fiber.abort("matched 51")
  } else if("52".contains(topic)) {
    Fiber.abort("matched 52")
  } else if("53".contains(topic)) {
    Fiber.abort("matched 53")
  } else if("54".contains(topic)) {
    Fiber.abort("matched 54")
  } else if("55".contains(topic)) {
    Fiber.abort("matched 55")
  } else if("56".contains(topic)) {
    Fiber.abort("matched 56")
  } else if("57".contains(topic)) {
    Fiber.abort("matched 57")
  } else if("58".contains(topic)) {
    Fiber.abort("matched 58")
  } else if("59".contains(topic)) {
    Fiber.abort("matched 59")
  } else if("60".contains(topic)) {
    Fiber.abort("matched 60")
  } else if("61".contains(topic)) {
    Fiber.abort("matched 61")
  } else if("62".contains(topic)) {
    Fiber.abort("matched 62")
  } else if("63".contains(topic)) {
    Fiber.abort("matched 63")
  } else if("64".contains(topic)) {
    Fiber.abort("matched 64")
  } else if("65".contains(topic)) {
    Fiber.abort("matched 65")
  } else if("66".contains(topic)) {
    Fiber.abort("matched 66")
  } else if("67".contains(topic)) {
    Fiber.abort("matched 67")
  } else if("68".contains(topic)) {
    Fiber.abort("matched 68")
  } else if("69".contains(topic)) {
    Fiber.abort("matched 69")
  } else if("70".contains(topic)) {
    Fiber.abort("matched 70")
  } else if("71".contains(topic)) {
    Fiber.abort("matched 71")
  } else if("72".contains(topic)) {
    Fiber.abort("matched 72")
  } else if("73".contains(topic)) {
    Fiber.abort("matched 73")
  } else if("74".contains(topic)) {
    Fiber.abort("matched 74")
  } else if("75".contains(topic)) {
    Fiber.abort("matched 75")
  } else if("76".contains(topic)) {
    Fiber.abort("matched 76")
  } else if("77".contains(topic)) {
    Fiber.abort("matched 77")
  } //last endif
} //iter
