for(iter in 1..2000) {
  var topic = "a" // can't possibly match
  if("1".contains(topic)) {
    1            //Fiber.abort("matched 1")
  } else  if("2".contains(topic)) {
    1            //Fiber.abort("matched 2")
  } else  if("3".contains(topic)) {
    1            //Fiber.abort("matched 3")
  } else  if("4".contains(topic)) {
    1            //Fiber.abort("matched 4")
  } else  if("5".contains(topic)) {
    1            //Fiber.abort("matched 5")
  } else  if("6".contains(topic)) {
    1            //Fiber.abort("matched 6")
  } else  if("7".contains(topic)) {
    1            //Fiber.abort("matched 7")
  } else  if("8".contains(topic)) {
    1            //Fiber.abort("matched 8")
  } else  if("9".contains(topic)) {
    1            //Fiber.abort("matched 9")
  } else  if("10".contains(topic)) {
    1            //Fiber.abort("matched 10")
  } else  if("11".contains(topic)) {
    1            //Fiber.abort("matched 11")
  } else  if("12".contains(topic)) {
    1            //Fiber.abort("matched 12")
  } else  if("13".contains(topic)) {
    1            //Fiber.abort("matched 13")
  } //last endif
} //iter
